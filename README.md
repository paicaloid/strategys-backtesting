# strategys-backtesting

## 1. pre-installation
```
conda install -c conda-forge libta-lib
pip install -U "vectorbtpro[base] @ git+https://github.com/polakowo/vectorbt.pro.git"
pip install -r requirements.txt
```